# emojicrypt
EmojiCrypt is a lightweight web tool that encodes and decodes hidden messages using emoji-based encryption. Built with HTML, CSS, and JavaScript, it runs entirely client-side with no server required.
